# Dependencies
node_modules/
.wrangler/

# Build artifacts (regenerated by `npm run build`)
# public/ar/index.html and public/api/catalog.json ARE committed so the
# site can be deployed without a D1 connection at first; rebuild them
# via `npm run build` after dashboard edits.

# Local environment
.env
.env.local
*.local

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*

# Cloudflare
.dev.vars
